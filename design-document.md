# Webアプリ仕様書

## 1. 概要
英語の文章、PDF、画像を読み込んで、日本語訳・要約・重要単語を確認できる英語学習アプリ。
文章は複数保存でき、文章ごとに単語帳を作れるようにする。

## 2. 主な機能

### 2.1 入力機能
- テキスト、PDF、画像から入力方法を選べるようにする。
- PDFは文字PDFと画像PDFの両方に対応する。
- 読み込んだ文章は保存し、あとから一覧で選べるようにする。

### 2.2 翻訳・要約機能
- 英文を日本語訳に変換する。
- 文章全体の要約を表示する。
- 英文と日本語訳を並べて表示する。
- 文章表示エリアはスクロールできるようにする。

### 2.3 単語学習機能
- 重要単語を自動で抜き出す。
- 単語一覧は文章の最初か最後にまとめて表示する。
- 単語をクリック、またはマウスを乗せたときに意味を表示できるモードを作る。
- 文章ごとに単語帳を作成できるようにする。
- 単語帳はクイズ形式で復習できるようにする。
- クイズでは「分かった」は右、「分からなかった」は左に分けるような操作にする。

### 2.4 設定機能
- 右上のハンバーガーメニューから設定を開けるようにする。
- 簡単すぎる単語が出ないように、単語抽出の難易度を設定できるようにする。

## 3. 使用技術
| 分類 | 技術・ライブラリ |
| --- | --- |
| フロントエンド | Vue.js / HTML5 + CSS3 |
| バックエンド | Flask |
| 通信方式 | Fetch API / JSON |
| AI | Ollama または OpenRouter |

## 4. 入出力例

### 入力
- 英文テキスト
- PDFファイル
- 画像ファイル

### 出力
- 日本語訳
- 要約
- 重要単語一覧
- 文章ごとの単語帳
- 単語クイズ

## メモ
- 左側にChatGPTのようなチャット一覧を表示する。
- 中央に英語、右側に日本語訳を表示する。
- 内容理解クイズ、読了時間、難しさ表示は一旦入れない。
