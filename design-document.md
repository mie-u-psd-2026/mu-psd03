# デザインドキュメント

## 1. 概要
英語の文章、PDF、画像を読み込んで、日本語訳・要約・重要単語を確認できる英語学習アプリ。
文章は複数保存でき、文章ごとに単語帳を作れるようにする。

## 2. 入力と保存
- 入力方法はテキスト、PDF、画像から選べるようにする。
- PDFは文字PDFと画像PDFの両方に対応する。
- 読み込んだ文章は保存して、あとから一覧で選べるようにする。
- 各文章に日本語訳、要約、重要単語、単語帳をひもづける。

## 3. 学習機能
- 英文と日本語訳を並べて表示する。
- 単語をクリック、またはマウスを乗せたときに意味を表示できるモードを作る。
- 文章全体の要約を表示する。
- 重要単語を自動で抜き出し、文章の最初か最後にまとめて表示する。
- 文章ごとに単語帳を作成できるようにする。
- 単語帳はクイズ形式で復習できるようにする。
- クイズでは「分かった」は右、「分からなかった」は左に分けるような操作にする。
- 簡単すぎる単語が出ないように、単語抽出の難易度を設定できるようにする。
- 内容理解クイズ、読了時間、難しさ表示は一旦入れない。

## 4. 画面構成
- 左側にChatGPTのようなチャット一覧を表示する。
- 中央に英語、右側に日本語訳を表示する。
- 単語一覧は画面の上か下に表示する。
- 単語クイズ、単語帳作成ボタンを置く。
- 右上のハンバーガーメニューから設定を開けるようにする。
- 文章表示エリアはスクロールできるようにする。
- 参考画像は `images/` フォルダに置く。
  - `images/text-translation-layout.png`
  - `images/chat-vocabulary-layout.jpg`
